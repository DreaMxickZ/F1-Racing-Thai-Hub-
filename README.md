# F1 Racing Hub 🏎️

เว็บไซต์รวบรวมข้อมูล Formula 1 ฤดูกาล 2026 และต่อๆ ไป พร้อมระบบจัดการข่าวสารและข้อมูลนักแข่ง

## ฟีเจอร์หลัก

### หน้าสาธารณะ
- 📰 **หน้าแรก** - แสดงข่าวสาร, ตารางคะแนน, และการแข่งขันถัดไป
- 🏁 **นักแข่ง** - รายชื่อนักแข่งทั้งหมด พร้อมรูปภาพและข้อมูล
- 🏎️ **ทีม** - รายชื่อทีมทั้งหมด
- 🏟️ **สนาม** - ข้อมูลสนามแข่งทั้งหมด พร้อม Layout และพิกัด
- 📅 **ตารางแข่ง** - ตารางการแข่งขันทั้งฤดูกาล พร้อมรอบต่างๆ และจำนวนรอบแข่ง
- 🏆 **คะแนน** - ตารางคะแนนนักแข่งและทีม

### ระบบ Admin (มีระบบ Login ป้องกัน 🔐)
- 🔐 **Login/Logout** - เข้าสู่ระบบด้วย Email และ Password
- ✍️ **จัดการข่าว** - เพิ่ม/แก้ไข/ลบข่าวสาร
- 👤 **จัดการนักแข่ง** - อัพโหลดรูปนักแข่ง, รูปรถ, หมายเลข
- 🖼️ **อัพโหลดรูปภาพ** - ระบบจัดการรูปภาพผ่าน Supabase Storage

## เทคโนโลยีที่ใช้

### Frontend
- **React 18** + **Vite** - Fast development
- **TailwindCSS** - Styling (โทนสี F1 แดง-ดำ-ขาว)
- **React Router** - Navigation
- **Lucide React** - Icons

### Backend & Database
- **Supabase** - Database, Authentication, Storage
  - PostgreSQL database
  - Storage สำหรับรูปภาพ
  - Row Level Security

### APIs
- **OpenF1 API** - ข้อมูล live timing, sessions, laps
- **Jolpica F1 API** - ข้อมูลทั่วไป (ทีม, นักแข่ง, ตารางแข่ง, คะแนน)

## การติดตั้ง

### 1. Clone Repository
```bash
git clone <repository-url>
cd f1-website
```

### 2. ติดตั้ง Dependencies
```bash
npm install
```

### 3. ตั้งค่า Supabase

#### สร้าง Project ใน Supabase
1. ไปที่ [supabase.com](https://supabase.com)
2. สร้าง Project ใหม่
3. คัดลอก Project URL และ anon key

#### สร้าง Tables

**Table: news**
```sql
CREATE TABLE news (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  title TEXT NOT NULL,
  content TEXT NOT NULL,
  image_url TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

**Table: drivers**
```sql
CREATE TABLE drivers (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  driver_id TEXT UNIQUE NOT NULL,
  number TEXT,
  team TEXT,
  image_url TEXT,
  car_image_url TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

#### สร้าง Storage Bucket
1. ไปที่ Storage ใน Supabase Dashboard
2. สร้าง bucket ชื่อ `images`
3. ตั้งค่าเป็น Public bucket

#### ตั้งค่า Authentication (สำคัญ!)
1. ไปที่ **Authentication** → **Users**
2. สร้าง Admin user แรก:
   - คลิก "Add user" → "Create new user"
   - Email: `admin@yourdomain.com`
   - Password: รหัสผ่านที่แข็งแรง
   - ✅ Auto Confirm User
3. **อ่านรายละเอียดเพิ่มเติมใน [AUTH_SETUP.md](./AUTH_SETUP.md)**

### 4. ตั้งค่า Environment Variables
สร้างไฟล์ `.env` จาก `.env.example`:
```bash
cp .env.example .env
```

แก้ไขไฟล์ `.env`:
```
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### 5. รันโปรเจค
```bash
npm run dev
```

เปิดเบราว์เซอร์ที่ `http://localhost:3000`

## การ Deploy

### Vercel (แนะนำ)
1. Push โค้ดไปที่ GitHub
2. ไปที่ [vercel.com](https://vercel.com)
3. Import repository
4. เพิ่ม Environment Variables:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
5. Deploy!

### Netlify
1. Push โค้ดไปที่ GitHub
2. ไปที่ [netlify.com](https://netlify.com)
3. Import repository
4. Build command: `npm run build`
5. Publish directory: `dist`
6. เพิ่ม Environment Variables
7. Deploy!

## โครงสร้างโปรเจค

```
f1-website/
├── src/
│   ├── components/          # Components ที่ใช้ซ้ำ
│   │   ├── Navbar.jsx
│   │   └── Footer.jsx
│   ├── pages/              # หน้าต่างๆ
│   │   ├── Home.jsx
│   │   ├── Drivers.jsx
│   │   ├── Schedule.jsx
│   │   ├── Standings.jsx
│   │   └── admin/          # หน้า Admin
│   │       ├── AdminDashboard.jsx
│   │       ├── NewsManagement.jsx
│   │       ├── NewsForm.jsx
│   │       └── DriverManagement.jsx
│   ├── services/           # API services
│   │   └── f1Api.js
│   ├── config/             # Configuration
│   │   └── supabase.js
│   ├── App.jsx             # Main App
│   ├── main.jsx            # Entry point
│   └── index.css           # Global styles
├── public/                 # Static files
├── package.json
├── vite.config.js
├── tailwind.config.js
└── README.md
```

## API Documentation

### OpenF1 API
- **Base URL**: `https://api.openf1.org/v1`
- **ฟรี** ไม่ต้อง API key
- [Documentation](https://openf1.org/)

### Jolpica F1 API
- **Base URL**: `https://api.jolpi.ca/ergast/f1`
- **ฟรี** ไม่ต้อง API key
- แทน Ergast API ที่ปิดไปแล้ว
- [GitHub](https://github.com/jolpica/jolpica-f1)

## คู่มือการใช้งาน

### การเข้าสู่ระบบ Admin
1. คลิก **"เข้าสู่ระบบ"** ที่มุมบนขวา (หรือไปที่ `/login`)
2. ใส่ Email และ Password ที่สร้างไว้ใน Supabase
3. คลิก "เข้าสู่ระบบ"
4. เมนู Admin จะปรากฏใน navbar

### การเพิ่มข่าว
1. เข้าสู่ระบบก่อน (ต้อง login)
2. ไปที่หน้า Admin (`/admin`)
3. คลิก "จัดการข่าวสาร"
4. คลิก "เพิ่มข่าวใหม่"
5. กรอกหัวข้อและเนื้อหา
6. อัพโหลดรูป (หรือใส่ URL)
7. คลิก "บันทึก"

### การแก้ไขข้อมูลนักแข่ง
1. เข้าสู่ระบบก่อน (ต้อง login)
2. ไปที่หน้า Admin (`/admin`)
3. คลิก "จัดการนักแข่ง"
4. เลือกนักแข่งจากรายการด้านซ้าย
5. กรอกข้อมูล:
   - หมายเลขนักแข่ง
   - ทีม
   - อัพโหลดรูปนักแข่ง (ยืนข้างรถ)
   - อัพโหลดรูปรถ
6. คลิก "บันทึกข้อมูล"

### การออกจากระบบ
1. คลิก **"ออกจากระบบ"** ที่มุมบนขวา
2. จะกลับไปหน้าแรกและเมนู Admin จะหายไป

## สีที่ใช้ (F1 Theme)

- **F1 Red**: `#E10600`
- **F1 Black**: `#15151E`
- **F1 Gray**: `#38383F`
- **Light Gray**: `#949498`
- **White**: `#FFFFFF`

## ฟีเจอร์ที่จะเพิ่มในอนาคต

- [ ] ระบบ Authentication สำหรับ Admin
- [ ] หน้าทีม (Teams page)
- [ ] หน้ารายละเอียดสนาม
- [ ] Live Timing จาก OpenF1 API
- [ ] Lap times comparison
- [ ] การแจ้งเตือนการแข่งขัน
- [ ] Dark/Light mode toggle
- [ ] Mobile responsive improvements
- [ ] ระบบค้นหา
- [ ] SEO optimization

## License

MIT License

## ผู้พัฒนา

พัฒนาโดยใช้ Claude (Anthropic) 🤖

---

สนุกกับการติดตาม F1! 🏁
